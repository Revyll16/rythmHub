<div id="feedback-<%= feedback.id %>">
  <small>
    <strong><%= feedback.musician.name %></strong>
    <i><%= feedback.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= feedback.content %></p>
</div>
